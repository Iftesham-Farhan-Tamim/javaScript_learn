// Immediately Invoked Function Expressions (IIFE)
// <(;)> MUST


(function chai() {
    console.log(`DB CONNECTED`);  // DB CONNECTED 
})();


((name) => {
    console.log(`DB CONNECTED ${name}`);  // DB CONNECTED Tamim
})("Tamim");




